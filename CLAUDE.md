이 저장소는 SPEC.md의 사양을 구현한다.
작업 전 PROGRESS.md(진행 상황·개발 환경·다음 작업)와 SPEC.md를 읽을 것.
사양과 다르게 구현해야 할 이유가 생기면 코드를 바꾸기 전에 먼저 질문할 것.

SPEC.md와 이 지침은 API를 직접 확인하기 전에 세운 계획이다.
실제 응답·데이터가 사양과 다르면 근거(샘플과 수치)를 들어 제안하고,
승인되면 SPEC.md 본문과 끝의 "변경 이력"을 함께 고친다.
작업을 마칠 때 PROGRESS.md를 갱신한다.

특히 아래는 임의로 바꾸지 말 것:
- 전용면적 기준 통일 (공급면적 금지)
- 취소거래 제외 기본값
- 단지 식별은 국토부 단지 일련번호(aptSeq). 단지명 매칭 금지
  (지번 키는 10자리 법정동코드+본번+부번이며 건축물대장 조회용.
  시군구+본번+부번만으로는 단지가 구분되지 않는다 — SPEC 변경 이력 참조)
- 실거래가는 apis.data.go.kr/1613000/ 엔드포인트만 사용
  (openapi.molit.go.kr 구 엔드포인트 금지)
- 매매는 상세 자료(15126468) 사용, 일반 자료(15126469) 금지
- 건축물대장은 건축HUB(15134735) 사용, BldRgstService_v2 금지
- 인증키는 디코딩 키 사용
- MCP 도구에 범용 SQL 실행 노출 금지
- 모든 집계 응답에 sample_size와 data_completeness 포함
